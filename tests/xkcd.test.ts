// import { add } from '../src/xkcd';

describe("Not really a test", () => {
  test("Dummy test", () => {
    expect(1 === 1).toBe(true);
  });
  // test('Testing add()', () => {
  //   expect(add(4,5)).toBe(9);
  // });
});
